/*window.addEvent('domready', function() {
	var submissionAreas = $$('.tl_formbody_submit');
	
	Array.each(submissionAreas, function(element){
		new Drag(element, {
			onStart: function(el) {
				el.set('styles', {right: 'auto'});
			}
		});
	});
});*/